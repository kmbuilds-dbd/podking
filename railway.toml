[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uv run alembic upgrade head && uv run uvicorn podking.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
