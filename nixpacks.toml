providers = ["node", "python"]

[phases.install]
cmds = [
    "cd frontend && npm ci --omit=dev",
    "uv sync --no-dev",
]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "uv run alembic upgrade head && uv run uvicorn podking.main:app --host 0.0.0.0 --port $PORT"
